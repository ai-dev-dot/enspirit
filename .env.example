# 以下变量可覆盖 docker-compose.yml 中的默认值（可选，不创建此文件也能直接使用）
# 如需自定义，创建 .env 文件并修改 docker-compose.yml 中对应行使用 ${变量名} 引用

# 数据库密码（默认: enspirit123）
DB_PASSWORD=your-strong-password-here

# 会话加密密钥（默认: 容器启动时自动生成随机字符串）
AUTH_SECRET=
